# Results

This directory is the local output target for generated demo metrics.

Generated metrics files, checkpoints, plots, and dataset-derived artifacts are ignored by Git unless deliberately promoted into the repository.
